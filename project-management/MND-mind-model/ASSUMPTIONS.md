# MND — Assumptions & Design Decisions

Non-obvious choices and the reasoning behind them. Updated whenever a decision changes.

| ID | Decision | Rationale | Affected area |
|----|----------|-----------|---------------|
| MND-001 | Mine only **user-authored turns** + the truncated last assistant text before each user turn. Skip tool results, command stdout, system reminders, sidechains. | Tomas's turns are the decision signal; the preceding assistant text is the context the decision answered. Tool results are noise AND where secrets (tokens, key material) overwhelmingly live — skipping them is the first line of secret hygiene. | extract |
| MND-002 | Regex redaction pass (ghp_/gho_, sk-, AKIA, JWT, op://, password/token assignments, PEM blocks) applied before moments are written to disk. | Sessions cover security/credential work. Nothing unredacted leaves the extract stage — not to disk, not to the LLM. | extract, security |
| MND-003 | LLM calls happen **host-side** via `npx @google/gemini-cli` with prompt files; the Go binary (in Docker) builds prompts and validates responses. Gemini default, Claude fallback. | Proven GML pattern (run-task.sh); keeps API auth out of the container; matches "Gemini is primary LLM" preference. | distill, profile, ask |
| MND-004 | Session dirs (`~/.claude/projects`, `~/.gemini/tmp`) are bind-mounted **read-only** into the container. | The pipeline must never be able to corrupt session history. | extract, docker |
| MND-005 | RAG = pure-Go **BM25 lexical retrieval** over `insights.yaml`, built in-memory at `ask` time. No vector DB, no embeddings, no persisted index in iteration 1. | KISS. Post-distillation corpus is small (hundreds of insights); lexical retrieval over short structured statements works; zero infra and zero keys. | ask |
| MND-006 | Profiles are **always included whole** in the ask prompt; BM25 retrieval only selects evidence/examples. | Profiles are the distilled core (small, few KB); retrieval risk only applies to the long tail. | ask |
| MND-007 | Orchestrator interface is a CLI (`mnd ask`, `--json`), not an HTTP service. | herdr agents can shell out. HTTP/DSH integration is iteration 2+ once answer quality is proven. | ask, herdr integration |
| MND-008 | **All production data** (profiles, insights, moments, ledgers) lives in gitignored `data/` and is persisted via encrypted `backup.sh`/`restore.sh`. Nothing is committed to git. | Originally `brain/` was committed (profiles/insights are post-redaction). Revised 2026-06-15 (MO §10): production data is gitignored and persisted via encrypted backup, driven by public-release preparation. Products run from master; `data/` is the product working, not a code change. | repo layout, security, backup |
| MND-009 | Distillation processes moments in batches with stable batch IDs; one malformed LLM item drops that item, not the batch. | GML lesson (iteration: "resilient validation — one bad LLM item no longer sinks the batch"). | distill |
| MND-010 | Insights carry evidence refs (source, session ID, timestamp, short quote) and an identity key for dedup across runs. | Claims without evidence can't be audited or corrected; identity-keyed dedup is the GML insight-dedup lesson. | distill, profile |
| MND-011 | gemini-cli is invoked with `--approval-mode default` (auto-denies all tool calls in non-interactive mode) and optional `MND_GEMINI_MODEL` pinning. | gemini-cli 0.29.5 gates `plan` behind `experimental.plan`. **`-e none` does NOT disable built-in tools** — with the user-level `defaultApprovalMode: auto_edit`, a 304KB profile prompt pushed gemini into writing files directly into the repo (caught live 2026-06-12, files quarantined and regenerated through validation). `default` mode can't prompt non-interactively, so every tool call is denied. Model pinning bypasses the auto-routing classifier, which chokes on >~300KB prompts. GML's `plan`-flag breakage flagged to Tomas; his call: fix later, GML works as-is. | run-task.sh, security |
| MND-012 | Extract collapses near-duplicate moment texts globally (normalized 200-char key) and drops machine-fed payloads (`--- FILE:` dumps, harness limit notices). | Live run 2026-06-12: 1014 of 2916 moments were templated batch-loop repeats and piped file content arriving as "user" turns — they would have skewed distillation toward one project's mechanics. | extract |
| MND-013 | herdr orchestration (`orchestrate.sh`) feeds the raw terminal tail as the question — no parsing of "what the agent asked". The LLM extracts the pending question itself; the prompt marks the tail as untrusted data. | KISS: terminal output formats vary by agent; the LLM is better at finding the question than a regex. Injection surface acknowledged: a hostile agent could try steering its own direction — acceptable while all agents are Tomas's own (iteration 2+: datamark the tail like distill does). | orchestrate.sh, security |
| MND-014 | **Delivery is the default** on both orchestrate CLIs; `--dry-run` is the shared preview flag. Targets: one pane, a selection, or all (`orchestrate-watch.sh [pane ...]`); explicit targets override `MND_WATCH_EXCLUDE`, the self-pane rule always holds. `confidence: low` still refuses to send and escalates to DSH. | Tomas's end state (2026-06-12): "the orchestrator provides continuous feedback to one, all, or selected panes" — after iteration 4's live verification, the safety valve is the confidence gate + pending gate + loop protections, not a default-off switch. (Original default-dry-run stood until trust was earned; revised same day it was.) | orchestrate.sh, orchestrate-watch.sh |
| MND-015 | Self/other discrimination is **turn-level**, not session-level: datamark fingerprint (U+E000), pipeline phrase markers, and the orchestrator send-ledger drop machine-authored turns; Tomas's turns survive wherever they appear — including inside agent-pane sessions. | Tomas's directions typed into agent panes are the highest-value signal; excluding whole sessions by location would discard them. The U+E000 datamark we inject as an injection defense is a perfect self-marker. The brain must never learn from its own output (recursion). | extract, exclude, retraining |
| MND-016 | `brain/processed.yaml` records every moment ID that went through distillation (success-merged batches only), independent of whether it yielded insights. | "Processed" inferred from evidence citations (iteration 1) re-sent ~930 silent moments per run. Explicit ledger = true increment for the retrain daemon; ledger holds only IDs, no content. | distill, retrain |
| MND-017 | Feedback insights: `strength: strong`, `source: feedback`, evidence `dsh:<id>` with Tomas's comment as quote. The feedback ingest ledger records ALL gathered notifications — including zero-insight ones — so nothing is re-fetched. | A direct correction from Tomas is definitionally authoritative (the validity probe showed distilled inference losing to situational rulings). Ledgering only insight-yielding notifications would re-fetch one-off comments forever. | feedback, learn |
| MND-018 | MND has its **own** DSH OAuth client (Tomas's call, 2026-06-12); creds in gitignored `data/dsh.yaml` chmod 600, committed `dsh.yaml.example` template. Escalation/learn skip gracefully when the file is absent. | Per-service identity, independently revocable. Reusing GML's client was rejected — one revocation would kill both services. | dsh, security |
| MND-019 | Escalations use the `[MND ask <qhash>]` marker (normalized-question hash) as round-trip identity; re-asks update the active notification instead of reposting; learn-gather only ingests notifications carrying the marker. | GML iteration-021 lesson (identity-keyed update/skip). The marker also keeps MND's learn step from eating other projects' MND-tagged notifications. | feedback |
| MND-020 | LLM calls route through **LLP** when it's up (control-socket handshake, in-memory token, loopback API); auto-detected, `MND_LLP=off` opt-out, and any LLP failure falls back to the existing direct-CLI path unchanged. | Tomas's directive (2026-06-12): quota-aware best-model selection. LLP's router (per-impl queue, cooldown, failover) IS that logic, live-verified by GML the same day — MND must not duplicate infrastructure the team already runs. Auto-detect because the LLP instance is young (currently runs from a deleted worktree) — MND must work when it's down. | run-task.sh, llm routing |
| MND-021 | Highest-model selection is **server-side**: MND sends `model: auto` and LLP's capability ladder (tiered impls, top claude tier now `claude-opus-4-8`, per-tier quota cooldowns) picks the most capable model with quota. `--model claude` preference maps to chain `claude auto`; `MND_LLP_CHAIN` overrides. Direct-CLI claude fallback pinned `claude-opus-4-8`. | "Use the highest models… check the quotas… choose the best" (Tomas) belongs in ONE place — LLP. **2026-06-13: Anthropic pulled `claude-fable-5`** (now exits non-zero "currently unavailable"); LLP fails over correctly — verified live, `model:auto` served by gemini while the dead claude rung 502s and is skipped — but the top claude pin was bumped fable-5 → opus-4-8 in run-task.sh and the LLP config still needs the same one-line change + restart (Tomas-only). | run-task.sh, ask quality |
| MND-022 | Watch mode answers a given (pane, normalized-tail-hash) **once**; the same question reappearing after an answer escalates to DSH instead of resending. Self-pane and `MND_WATCH_EXCLUDE` panes are skipped. | Loop protection: resending an answer that didn't unblock the agent would ping-pong forever; a repeat means the mind model's direction failed — that's Tomas's case by definition. Self-exclusion prevents the orchestrator answering itself (recursion, MND-015's runtime twin). | orchestrate-watch.sh |
| MND-023 | The ask response carries `pending: question\|none`; orchestrate skips sending when `none`. | Idle agents often finished cleanly and ask nothing — sending fabricated "direction" would inject noise into healthy agents. The LLM already extracts the pending question (MND-013); making that explicit is one field, not a parser. | ask, orchestrate |
| MND-024 | Every direction `orchestrate.sh` delivers into a pane is prefixed `[MND orchestrator]` (env `MND_SEND_PREFIX`). The prefix is also an `internal/exclude` phrase-marker and is included in the sent-ledger hash. | Tomas (2026-06-13): the conversation must visibly show when MND — not Tomas — is answering. Marking the delivered text also closes the recursion risk by construction: any captured turn carrying the prefix is dropped at extract, so retraining never relearns MND's own output. The marker default and the exclude phraseMarker must stay in sync. | orchestrate.sh, exclude, retraining |
| MND-025 | Contradiction resolution, **three-way verdict** per flagged set (`mnd contradiction-prompt`/`-merge`): the LLM classifies each tension as **`contradiction`** (same context, opposite — Go retires the loser by provenance: **feedback > distill, newer > older, strong > weak**, stable id tiebreak; loser marked `status: superseded` + `superseded_by` + reason, kept for audit) or **`context_split`** (both valid in different situations — nothing retired, each member's `context` is sharpened so they stop colliding). Unknown/missing verdict defaults to `context_split` (non-destructive). `ask` honors context scoping (rule 1a). Superseded excluded from profiles/ask via `distill.Active()`. Runs in `retrain` after `learn`. | Binary conflict→retire over-retired context-scoped tech defaults (live 2026-06-13: "run services in containers" vs "build locally" are different contexts, not a contradiction — Tomas's call). The three-way verdict reserves retirement for genuine same-context conflicts and turns the common case into a scoping. **Limits (live): (a) only retires when a contradicting insight EXISTS — the "don't use branches" relic persists, no insight defeats it; (b) coverage is non-deterministic — the env-var-credentials contradiction was caught one run, missed the next; repeated retrain converges, a single pass does not guarantee.** | contradiction, distill, ask, profile, retrain |
| MND-026 | The branch-per-project workflow (worktrees + branch, master integration-only, MO §10) is encoded as a `source: feedback` insight — authoritative, so the contradiction sweep retires the stale "work directly on master, no branches" relic. | Contradiction resolution can only retire a belief when a contradicting insight exists (MND-025 limit). The workflow was adopted by instruction + research, never distilled — so it was added by hand as feedback, the same authority class as a DSH correction. | brain, contradiction |
| MND-028 | The brain is **production data**, not code, so it lives on master (MO §10 "code vs production data", Tomas 2026-06-14). `retrain` on master **commits** its learning to master (local, never pushed); in a worktree the brain output is **dev/test data** — left local, never merged into master via a code branch. (Supersedes the earlier "refuse retrain on master" guard, which had the model backwards: production learning belongs on master, it just must be *committed*, not left uncommitted — the daemon's actual bug, 2026-06-13.) | Production learning is the product working; gating it behind code review would freeze the brain. The 2026-06-13 daemon failure was leaving output uncommitted in the integration checkout, not running there. | run-task.sh, retrain, workflow |
| MND-029 | Contradiction sweep is **loop-until-dry** (`run-task.sh contradictions`): repeat until `MND_SWEEP_DRY` (default 2) consecutive passes change nothing, capped at `MND_SWEEP_MAX` (default 4). `context_split` is **idempotent** — an insight is scoped only if it has no context yet (the LLM re-flags the same pairs and rewords cosmetically every pass; rewriting each time means the brain never stabilizes). | A single sweep's coverage is non-deterministic — genuine contradictions surface on different passes (live 2026-06-14: a retirement appeared only on pass 3). Looping catches them; idempotent scoping makes it converge instead of thrash; the cap bounds per-run token cost (successive retrains keep chipping). | contradiction, run-task.sh |
| MND-030 | `WriteProfiles` repairs raw control chars inside JSON string literals (`repairJSONStringNewlines`) before parsing. | gemini-cli recurrently emits multiline Markdown with unescaped newlines inside JSON string values — invalid JSON that broke the iter-6 profile regen and would break every recurring retrain's profile step. Escape-and-retry makes the hands-off loop reliable on either model. | brain, profile, retrain |
| MND-031 | Fidelity eval (`mnd eval`, `internal/eval`): sample real decision moments → frame ground-truth cases (LLM) → clone answers BLIND (gold never in the prompt, T52) → independent LLM judge (agree/partial/disagree, junk⇒disagree) → report (fidelity %, per-category, confidence calibration, disagreement work-list). In-sample by default (labeled optimistic upper bound); `--held-out` train/test eval-brain deferred. | "Make me redundant" needs a measure, not anecdotes. The disagreement list (where the clone is wrong) targets retraining; the calibration table tests whether confidence predicts correctness. First large run (43 cases): 59% in-sample, and **confidence was 100% `high` while 41% wrong** — the report now flags non-discriminating confidence, since the safety gate relies on it. | eval, ask, retrain |
| MND-033 | **Mandatory post-retrain fidelity eval** (`eval` + `route-eval` + `fidelity-check` in every `retrain` run). If auto-set delivered fidelity drops below `MND_FIDELITY_MIN_AUTO` (default **75%**, approved by Tomas 2026-06-15) or judgment questions leak into auto-answer, a DSH `action_needed/Q1` alert fires. The brain keeps working but Tomas knows it degraded. | The brain evolves continuously via retraining; without re-measurement, routing policy is frozen at iteration 10's snapshot while fidelity may shift. Also catches regressions: new insights could dilute the 80% tech fidelity or the 86% correction fidelity, and confidence is useless for detecting this (always `high`). Tomas: "it should scream if the fidelity changed below what's accepted." | retrain, eval, route, dsh |
| MND-034 | **Embedding-based retrieval + evidence gate** (iter 11, replaces MND-005 BM25 + MND-032 classifier): insights are pre-embedded via Ollama (`nomic-embed-text`, 768-dim, local GTX 1080 GPU). At ask time, the question is embedded and cosine top-k retrieves semantically relevant insights. The evidence gate inspects the retrieved set (dominant category, mean/min similarity, dominance fraction) instead of running an LLM classifier. Auto-set expanded to `correction_pattern,direction_pattern,tech_preference` (91% fidelity, 57% coverage, measured on 21 eval cases). Fallback: if no embeddings available, legacy classify gate. BM25 code stays for the ask prompt (not yet replaced for LLM context). | BM25 saturated on ~800 lexically homogeneous insights (iter 9: topScore identical right vs wrong). LLM classifier didn't generalize (49% → 11% on fresh questions). Embedding retrieval discriminates by meaning: correction question → 75% correction insights; tech → 92% tech; judgment → 83% judgment. Tech re-included in auto-set: gold-tech fidelity was always 80%; the prior exclusion was due to classifier mislabeling, not brain incompetence. Measured: adding tech raised fidelity 87% → 91% with +19% coverage. Tomas approved 2026-06-16. | embed, ask, route, orchestrate.sh, retrain |
| MND-032 | **Competence-boundary routing** (`internal/route`, iter 10): the orchestrator's safety valve is the question's CATEGORY, not confidence. A cheap LLM classifies the incoming question (`run-task.sh classify`); `orchestrate.sh` auto-answers categories in `MND_ROUTE_AUTO` (default `correction_pattern,direction_pattern`) and escalates the rest to Tomas — even at confidence `high`. `MND_ROUTE=off` = legacy answer-everything. The routing signal is validated **externally** (judge verdicts on eval cases), never self-reported; the classifier's own accuracy is measured against gold. | Tomas (2026-06-15): "do some testing about the measurable part — self-measure/confidence was totally off." Two prior measures failed (LLM confidence uniformly `high`; lever-A retrieval-confidence non-discriminating), so the gate cannot key on either. **Measured (43 cases, in-sample, against the production baseline):** the *predicted* category discriminates (correction 86% → judgment 42%) where confidence did not; auto-answering correction(+direction) delivers 78–86% vs 59% blanket with **0 judgment leaks**. Counter-finding: route on *predicted* not *gold* — gold-tech is 80% but pred-tech only 50% (classifier 49% accurate, over-assigns tech), so tech escalates. Honest cost: 58–74% of decisions escalate to Tomas — correct, since the clone is trustworthy on only ~30–40%. Caveat: in-sample upper bound, small per-bucket n; held-out (MND-031) is the next rigor step. | route, orchestrate.sh, orchestrate-watch.sh, ask |
