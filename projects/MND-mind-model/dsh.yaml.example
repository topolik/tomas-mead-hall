# MND → DSH client config. Copy to data/dsh.yaml (gitignored), chmod 600,
# and fill credentials from the DSH admin panel: /admin/clients.
url: "http://localhost:9090"
client_id: ""
client_secret: ""
